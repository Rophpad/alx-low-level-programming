#include <stdio.h>

/**
 * main - Prints a text using the function printf()
 * Return: 0 (EXIT_SUCCESS)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
