#include "main.h"

/**
 * set_string - sets the value of a pointers to char
 * @s: a pointers to a pointer that point to a char
 * @to: a pointer to the char
 *
 */
void set_string(char **s, char *to)
{
	*s = to
}
